# Lambent
A Python library for dynamically injecting try/except blocks into functions using AST manipulation.
### What it does
Lambent lets you wrap specific line ranges in a function with exception handling after the function is defined. No decorators, no manual try/except blocks. Just specify the range and an error handler.
